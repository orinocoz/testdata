- name: Restart bind
  ansible.builtin.service:
    name: bind9
    state: restarted

- name: Reload rndc
  shell: rndc reload

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

