#- name: Ansible ping module
#  ansible.builtin.ping:

# Update APT cache
- name: Update APT cache
  ansible.builtin.apt:
    update_cache: yes
    cache_valid_time: 86400

## Create user juri
#- name: Create user juri
#  ansible.builtin.user:
#    name: juri
#    state: present
#   shell: /bin/bash

## Add public key for user juri from github
#- name: Add public key for user juri from github
#  ansible.builtin.authorized_key:
#    user: juri
#    state: present
#    key: https://github.com/hudolejev.keys

## Create user roman
#- name: Create user roman
#  ansible.builtin.user:
#    name: roman
#    state: present
#    shell: /bin/bash

## Add public key for user roman from github
#- name: Add public key for user roman from github
#  ansible.builtin.authorized_key:
#    user: roman
#    state: present
#    key: https://github.com/romankuchin.keys

- name: Update APT cache
  ansible.builtin.apt:
    cache_valid_time: 86400

- name: Add Grafana APT GPG key
  ansible.builtin.apt_key:
    url: https://packages.grafana.com/gpg.key
    state: present

- name: Add Grafana APT repository (OSS)
  ansible.builtin.apt_repository:
    repo: "deb https://packages.grafana.com/oss/deb stable main"
    filename: grafana
    state: present

- name: Install Prometheus node 
  ansible.builtin.apt:
    name: prometheus-node-exporter
    state: present

- name: Ensure node_exporter are started and enabled
  ansible.builtin.service:
    name: prometheus-node-exporter
    state: started
    enabled: yes

- name: Install Promtail
  ansible.builtin.apt:
    name: promtail
    state: present

- name: Add Promtail user to adm group
  ansible.builtin.user:
    name: promtail
    groups: adm
    append: yes

- name: Ensure Promtail state directory exists
  ansible.builtin.file:
    path: /var/lib/promtail
    state: directory
    owner: promtail
    mode: '0755'

- name: Deploy Promtail configuration
  ansible.builtin.template:
    src: promtail.yaml.j2
    dest: /etc/promtail/config.yml
    owner: root
    group: root
    mode: '0644'
  notify: Restart Promtail

- name: Ensure Promtail service is started and enabled
  ansible.builtin.service:
    name: promtail
    state: started
    enabled: yes

