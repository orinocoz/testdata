# Memory consumption (percentage used) per VM
100 * (1 - (node_memory_MemAvailable_bytes / node_memory_MemTotal_bytes)) 
  by (instance)

# Average CPU load (percentage) per VM over the last 5 minutes
avg by (instance) (
  rate(node_cpu_seconds_total{mode!="idle"}[5m])
) * 100

